# frontend-dev 流程大纲（v1 · 用户已接受 2026-09-21 07:55）

主干：`1 需求理解 → 2 现状核对 → 3 编码实现 → 4 本地预览 → 5 自检 → 6 审查 → 7 收尾`
失败回环：5/6 不通过 → 回 3 修复；素材不足 → 回经验查询。

## 步骤

1. **需求理解**：定目标页面/改动类型（新增/修改/样式/交互/框架引入）与验收标准；读项目结构与上下文（双语目录映射、Article/News 模板范式），产出 change-plan。
2. **现状核对**：读取既有 HTML/CSS/JS 与资源引用；防重复样式、死链、覆盖写。
3. **编码实现**（四分支，完成后汇入 4）：
   - A 结构：语义化 HTML、meta/lang、双语导航同步
   - B 样式：移动优先响应式、主题变量；**依据 references 设计书籍原则**（栅格/层级/对比/间距）
   - C 交互：渐进增强 JS、微交互反馈；**依据交互设计书籍原则**（可供性/反馈/心智模型/费茨定律）
   - D 框架：Vue/React 与静态站嵌入协同
4. **本地预览**：静态服务 + `browser_interaction.py`（Playwright 截图/控制台；缺依赖降级 browser_open）。
5. **自检**：悬空链接、资源 404、HTML 有效性、双语同步完整性。
6. **审查**：衔接 `web-design-guidelines`（规范）+ `webapp-testing`（行为/axe 可访问性）。
7. **收尾**：变更清单汇报、提交建议、可选委托 `create-pull-request`。

## 用户增补（07:55）

知识库构建须含 **UI 设计 + 网页交互设计书籍**条目（原创摘要式笔记，注明出处，不复制原书正文），供 3B/3C/6 引用。

## 兜底

node/Playwright 缺失→纯 Python 静态检查；衔接技能未装→内置精简清单；无 git→提示后仅改文件；不可逆操作（push/覆盖）→先询问。

## 三查依据（内审已过）

- 可执行：每步映射现有工具或计划脚本；browser_interaction.py 有降级。
- 可达：主干线性、A–D 汇入 4、无孤立节点。
- 兜底：外部依赖均有降级路径；不可逆操作前置确认。
