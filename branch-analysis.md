# 分支分析记录（branch-analysis）

对 outline v1 各分支做约束/逻辑/结构/重复审核；依据链随文标注。

## 主干

- 1 需求理解 → 2 现状核对 → 3 编码实现 → 4 本地预览 → 5 自检 → 6 审查 → 7 收尾：线性可达，回环边仅 5/6→3（修复）与 3→经验补充（素材不足），无环死点。依据：outline.md 三查②。
- 重复行为审核：「双语同步」同时出现在 3A（导航）与 5（自检），属**实现-校验**关系非重复；「链接检查」出现在 2（读引用）与 5（遍历校验），同理保留。

## 编码分支（3 之下，汇入 4）

- A 结构：约束=语义化+lang+双语导航同步；兜底=找不到模板范式时询问用户并读同类页。
- B 样式：约束=移动优先+主题变量+禁大段内联；依据 references/design-books（栅格/层级/对比）；兜底=无设计系统时以现状站提取令牌。
- C 交互：约束=渐进增强+键盘可达；依据 design-books 交互原则（可供性/反馈/费茨）；兜底=JS 失效须保内容可读。
- D 框架：约束=不重写既有原生页、嵌入优先；兜底=无构建链时推荐 CDN/无构建方案并向用户确认。

## 视觉/听觉接口检查

- 需求判定仅「网页交互」→ 分支中保留 `scripts/browser_interaction.py` 接口；`vision_input.py`/`audio_input.py` 不建（依据 requirement.md 物理交互判断=否）。

## 衔接点

- 6 审查依赖外部技能 `web-design-guidelines`、`webapp-testing`；未安装→内置精简清单兜底（outline 兜底节）。
- 7 收尾可选委托 `create-pull-request`；无 git/gh 未登录→输出变更清单即止。
